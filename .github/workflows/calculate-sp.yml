name: Calculate SP and add comment

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

jobs:
  calculate-sp-and-comment:
    runs-on: ubuntu-latest

    steps:
      - name: Check if issue is updated
        id: check_issue
        uses: actions/github-script@v4
        with:
          script: |
            const updatedIssue = context.payload.issue;
            const updatedFields = updatedIssue && updatedIssue.fields;

            // Check if the issue fields are updated
            const isIssueUpdated = JSON.stringify(updatedFields) !== JSON.stringify(issue.fields);

            return { isIssueUpdated: isIssueUpdated };

      - name: Calculate SP and add comment
        id: calculate_sp
        uses: actions/github-script@v4
        with:
          script: |
            const { github, context, steps } = require("@actions/github");

            const issue = context.payload.issue;
            const fields = issue && issue.fields;

            // Check if the issue or fields are missing
            if (!issue || !fields) {
              console.log('Issue or fields are missing. Skipping SP calculation and comment addition.');
              return;
            }

            // ... Rest of the code ...

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
