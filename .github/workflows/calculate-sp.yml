name: Calculate SP and add comment

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

jobs:
  calculate-sp-and-comment:
    runs-on: ubuntu-latest

    steps:
      - name: Check if issue is updated
        id: check_issue
        uses: actions/github-script@v4
        with:
          script: |
            const updatedIssue = context.payload.issue;
            const updatedBody = updatedIssue && updatedIssue.body;
            const updatedFields = updatedIssue && updatedIssue.fields;

            // Check if the issue body or fields are updated
            const isIssueUpdated = updatedBody !== issue.body || JSON.stringify(updatedFields) !== JSON.stringify(issue.fields);

            return { isIssueUpdated: isIssueUpdated };

      - name: Calculate SP and add comment
        id: calculate_sp
        uses: actions/github-script@v4
        with:
          script: |
            const { github, context, steps } = require("@actions/github");

            const issue = context.payload.issue;
            const body = issue && issue.body;
            const fields = issue && issue.fields;

            // Check if the issue or body is missing
            if (!issue || !body) {
              console.log('Issue or body is missing. Skipping SP calculation and comment addition.');
              return;
            }

            // ... Rest of the code ...
